# ppqq-on-desk v0.10.1-ppqq.6

This prerelease adds another Q Pet idle animation.

Changes:

- Add `idle_belly_pat_dynamic.webp` as a second random idle animation for the built-in Q Pet theme.
- Normalize the belly-pat animation to the standard 192x208 runtime canvas and standing baseline.
- Bump the bundled Q Pet theme metadata to version `0.1.1`.
