# ppqq-on-desk v0.10.1-ppqq.1

This prerelease points builds and auto-update metadata at `pengpoom/ppqq-on-desk`.

Changes:

- Add periodic idle animations while the cursor is active.
- Map Codex tool failures and API errors to the error state.
- Publish release artifacts from the ppqq-on-desk repository.
